{
  "version": 2,
  "builds": [
    {
      "src": "api/main.go",
      "use": "@vercel/go",
      "config": {
        "buildCommand": "go build -o /vercel/path0/api/main"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/main.go"
    }
  ]
}
